# Variables

var x = 10
var name = "Alice"

# Conditional Statements:

if x > 5:
    print "x is greater than 5"
else:
    print "x is 5 or less"


# Loops - While Loop
var x = 10
while x > 0:
    print x
    x = x - 1

# Times loop

loop 5 times:
    print "Hello!"

# For Loops with Arrays

var numbers = [1, 2, 3, 4, 5]
print "Iterating through array:"
loop i in numbers:
    print i

# Array Examples

var empty_array = []
var mixed_array = ["hello", 42, true]
var nested_array = [[1, 2], [3, 4], [5, 6]]

# String Concatenation

var first_name = "Alice"
var last_name = "Smith"
var full_name = first_name ^ " " ^ last_name
print full_name

# Caret (^) operator for string concatenation (auto-converts to string)
var age = 30
print "Name: " ^ first_name
print "Age: " ^ age
print "Array: " ^ numbers
print "This is " ^ first_name ^ " who is " ^ age ^ " years old."

# Functions

func greet(name):
    print "Hello, " ^ name

greet("Alice")

# Function with array parameter
func calculate_sum(numbers):
    var sum = 0
    loop num in numbers:
        sum = sum + num
    return sum

var total = calculate_sum(numbers)
print "Sum of numbers: " ^ total

# Mathematical Operations

var sum = 5 + 3
print sum  // Outputs: 8
